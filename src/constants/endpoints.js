const API_BASE_URL = 'http://localhost:3004'

const ENDPOINTS = {
  EPISODES: `${API_BASE_URL}/films`,
  CHARACTERS: `${API_BASE_URL}/characters`,
  PLANETS: `${API_BASE_URL}/planets`,
}

export default ENDPOINTS
